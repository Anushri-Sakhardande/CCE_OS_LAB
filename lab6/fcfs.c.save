#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
typedef struct{
	int arrival_time;
	int burst_time;
	int pid;
	bool completed;
}process;

int main(){
	int time=0;
	int waiting_time[6];
	process order[5];
	int i,j;
	for(i=0;i<5;i++){
		printf("Enter the process burst time and id");
		scanf("%d",&order[i].burst_time);
		scanf("%d",&ord[i].pid);
		process[i].completed=false;
	}
	for(i=0;i<5;i++){
		int min=INT_MIN;
		for(j=0;j<5;j++){
			if(!process[j].completed && process[j].pid < min){
				min = j;
			}
		}
		process[min].completed=true;
		time=time + process[min].burst_time;
		waiting_time[process[min].pid]=time-process[min].arrival_time;
		printf("Process %d completed at %d\n",process[min].pid,time);
		printf("Waiting time %d\n\n",waiting_time[process[min].pid);
	}
	return 0;
}
